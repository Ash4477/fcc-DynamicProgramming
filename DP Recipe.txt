Memoization Recipe
    1. Make it work! (Create a simple solution)
        -> visualize problem as tree
        -> implement the tree using recursion
        -> test it
    2. Make it efficient!
        -> add a memo object
        -> add a base case to return memo values
        -> store return values in memo